1.解题思路1:
  遍历整个数组，每次获得一个数组的时候，判断一下另外的一个数(target - nums[i])在不在，用hash表保存下来


2.解题思路2:
  略


代码1：

class Solution {
public:
	vector<int> twoSum(vector<int>& nums, int target) {
		unordered_map<int, int> vmap;
				
		for (int i = 0; i < nums.size(); i++) {

			if (vmap.find(target - nums[i]) != vmap.end()) {
				return {vmap[target - nums[i]], i};
			}
			
			vmap[nums[i]] = i;

		}

		return {};
    }

};
